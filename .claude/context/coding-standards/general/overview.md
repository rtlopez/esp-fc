# General Coding Standards

**Scope:** Language-agnostic standards that apply across the repo.

Use the smallest entrypoint that fits the task:

- Full catalog: `./index-catalog.xml`
- Index metadata: `./index.xml`
